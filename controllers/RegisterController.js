var RegisterModel = require("../models/RegisterModel")

var createRegister = async (req, res) => {
    try {
        
    } catch (error) {
        console.log({ message: error.message })
    }
}

module.exports = { createRegister }